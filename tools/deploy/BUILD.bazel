load("@io_bazel_rules_docker//container:container.bzl", "container_image")

container_image(
    name = "proxy_image",
    base = "@envoy_base//image",
    directory = "/etc/envoy",
    files = [":envoy.yaml"],
)
